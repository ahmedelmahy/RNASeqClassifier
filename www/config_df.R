config_df <- data.frame(padj_max = .01,
                        lfc_min = 2,
                        cores = 1,
                        resampling_method = "cv",
                        num_genes = 2)
